## Summary

<!-- What does this PR change, and why? -->

## Requirement ID(s)

<!-- Which FR-2xx / NFR-2xx from docs/standards/task2-standards-pack.md does this touch?
     Example: FR-219 (task status transition). If this is a pure chore/docs change with no
     requirement mapping, say so explicitly instead of leaving this blank. -->

## Tests touched

<!-- Which test files/cases were added or changed? Did `make test` pass locally?
     Was `docs/openapi.json` regenerated with `make export-spec` if an endpoint changed? -->

## Screenshots

<!-- Required for any user-visible change (welcome page, Swagger UI, error responses, etc.).
     If no screenshot exists yet, write "manual step: capture <specific view>" instead of
     skipping this section. -->

## Checklist

- [ ] `make gate` passes locally, or CI is green on this PR
- [ ] `docs/openapi.json` is up to date (`make export-spec`) if an endpoint changed
- [ ] README updated if setup, configuration, or the endpoint list changed
- [ ] New architectural decisions are recorded under `docs/adr/`
