# Accessibility

This repository (Task 2) is a headless REST API — it has no user interface
(no HTML pages beyond a minimal landing/health check, no rendered forms, no
visual components) — so WCAG (Web Content Accessibility Guidelines) does not
apply to it, and this document intentionally makes no WCAG compliance claim
for this API; accessibility of the *user-facing* dashboard is covered by the
Task 1 dashboard repository's own accessibility statement, which this repo
does not duplicate or restate here.
